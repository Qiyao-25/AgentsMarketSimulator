# ============================================================
# 安装: pip install -r requirements.txt
# ============================================================

# --- 核心数据处理（必需）---
pandas>=1.5.0
numpy>=1.21.0

# --- HTTP 请求与进度条（数据获取和 API 调用必需）---
requests>=2.25.0
tqdm>=4.60.0

# --- LLM API 客户端（智能体决策必需）---
# 选手可使用 openai、anthropic 或其他 SDK。
# 基线代码基于 OpenAI 兼容接口（DeepSeek、OpenRouter 等均可）。
openai>=1.0.0

# --- 配置文件读取（推荐）---
# 用于读取 config.yaml 中的 LLM 和 Agent/交易所参数。
pyyaml>=6.0

# --- 测试（可选 -- 用于本地开发自测）---
# pytest 提供更丰富的测试输出
# pytest>=7.0.0
